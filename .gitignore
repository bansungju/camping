# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 동시 세션 핫파일 잠금 (런타임 상태 — 커밋 금지). 규칙은 WORK-LANES.md
.claude/locks/*.lock

# 로컬 서버/스크린샷/임시
*.log
.playwright-mcp/
site/.playwright-mcp/
*.png
.DS_Store

# 구 테스트 DB (현 산출물은 camping_tents500.db만)
camping.db
camping_auto.db

# 환경변수 / 시크릿 — 절대 커밋 금지
.env
.env.*
*.env
cf_token.txt
client_secret_*.json
# CF 터널 토큰 박힌 실제 plist (템플릿만 커밋)
launchdaemons/com.camping.cloudflared.plist

# 파이프라인 shadow DB (배포 전 임시)
camping_shadow.db
camping_shadow.db-wal
camping_shadow.db-shm

# 백업 폴더
camping_backup/

# 이미지: GitHub Pages 배포에 포함돼야 라이브에서 보임 → git 추적 필수.
# (gitignore 시 로컬 프리뷰만 되고 gear-forest.com에선 404. 현 76MB, Pages 1GB 한도 내)

# 임시 루프 계획서 (세션 분할용, 진행 중 갱신)
LOOP-PLAN-DATA.md
