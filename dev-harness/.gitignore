# ── dev-harness 런타임 산출물 (커밋 금지) ──
.checkpoints.db
.checkpoints.db-*
active.json
state.json
.ledger.lock
.dev-harness-wt/
scratch/
**/__pycache__/
*.pyc
*.pyo
*.tmp
.venv/
venv/

# ── 보안: 시크릿 절대 커밋 금지 (방어 심층화) ──
# 체크포인트 DB·워크트리엔 코드 초안 전문이 들어가므로 위에서 이미 차단.
# 아래는 누군가 dev-harness 하위에 시크릿 파일을 두는 사고 방지.
.env
.env.*
*.env
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
*credentials*.json
*secret*
*token*
auth.env
.envrc
service-account*.json
*.cred
